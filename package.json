{
  "name": "webmcp-local-agent",
  "version": "0.5.0",
  "private": true,
  "description": "Chrome MV3 extension: local Ollama chat with WebMCP tool calling.",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "lint": "node --check background.js && node --check content.js && node --check page-hook.js && node --check sidepanel.js && node --check lib/webmcp-schema.js"
  }
}
